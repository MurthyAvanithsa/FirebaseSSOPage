* Webpage to demonstrate SSO on android. 
This page is loaded while starting a long in the android app using chrome custom tab.
The page uses Google Identity platform SDK for web to authenticate a user using redirect flow.
On successful authentication it renders a button called "Back To app" which would open an android intent with IDToken.